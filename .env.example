# Copy to .env and fill in. Consumed by `make deploy` for the manual rollout.
# These mirror the Deploy workflow's secrets.
SSH_HOST=your.server.host
SSH_USER=deploy
SSH_PROJECT_PATH=/srv/monori
# Optional: path to the private key. Omit to use your ssh-agent / default keys.
SSH_KEY=~/.ssh/id_ed25519
